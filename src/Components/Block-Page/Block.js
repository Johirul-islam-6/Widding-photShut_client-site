import React from 'react';
import useTitle from '../../hook/useTitle';

const Block = () => {
    useTitle('Block')
    return (
        <div>
            this is block
        </div>
    );
};

export default Block;